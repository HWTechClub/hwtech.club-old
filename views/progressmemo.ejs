<%- include("partials/header", {title:title}); %>

<section class="home-content team-progress-header">
    <div class="row home-content__main">
        <h3><a href="/progress/<%= progress.team %>" ><i class="fa fa-chevron-left"></i> Back</a></h3>
        <h3><%= progress.date %></h3>
        <h1><%= progress.subject %></h1>
        <p><%= progress.memo %></p>
    </div>
</section>

<% include partials/footer %>